<%= form_for(@status_message) do |f| %>
  <% if @status_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status_message.errors.count, "error") %> prohibited this status_message from being saved:</h2>

      <ul>
      <% @status_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_area :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
